feat: proveedores portal, pdf reports, transferencias tab, and assistant tools

- Add public portal for suppliers (proveedores.tsx): licitaciones listing,
  detail view, offer creation with CUIT-based identity verification
- Add PDF report generation for licitaciones and transferencias with
  professional formatting (pdf-licitaciones-report.tsx,
  pdf-transferencias-report.tsx, pdf-reports.ts)
- Add server-side RPCs for public proveedores portal: list licitaciones,
  get detail, create offers, identify by CUIT
- Add Transferencias tab inside licitaciones page with full table of
  cross-hospital transfers, status badges, and warehouse name resolution
- Add persistent chat history to demo-assistant-chat and
  LicitacionesAssistant (sync user/assistant messages to Zustand store)
- Add find_similar_licitaciones tool: checks active licitaciones for
  overlapping medications before creation to prevent duplicates
- Add create_transfer tool to demo-assistant-chat for cross-hospital
  stock transfers with user confirmation
- Enrich LicitacionesAssistant prompt with active licitacion details,
  overstock summary, and transfer suggestion rules
- Fix duplicate codigo on licitacion creation: server auto-generates
  unique code when provided one already exists
- Remove Groq provider option — lock assistant to OpenCode Zen
  (deepseek-v4-flash-free)
- Update dependencies and route tree
