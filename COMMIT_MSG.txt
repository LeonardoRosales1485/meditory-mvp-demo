feat: licitaciones assistant, transferencias tab, chat persistence, and transfer filters

- Add Transferencias tab inside licitaciones page (right of Proveedores)
  with full table of transfers across hospitals, status badges, and
  origin/destination warehouse resolution
- Add persistent chat history to demo-assistant-chat (was reading from
  store but never writing back) and LicitacionesAssistant (had no
  persistence at all) — both now sync user/assistant messages to
  shared Zustand store
- Add find_similar_licitaciones tool that checks active licitaciones
  for overlapping medications before allowing creation, preventing
  duplicate purchase requests
- Add server-side findSimilarLicitaciones(medicationIds) function +
  RPC, tool definition, parser, and handlers in both assistants
- Enrich LicitacionesAssistant system prompt with active licitacion
  details (codigo, titulo, estado) and overstock summary for transfer
  suggestions
- Add create_transfer tool to demo-assistant-chat (tool + parser
  already existed in shared module): the assistant can now plan and
  create stock transfers between hospitals with user confirmation
- Add transfer planning rules to demo-assistant system prompt and
  transfer suggestion rules to licitaciones assistant prompt
- Fix duplicate codigo error on licitacion creation: server now
  checks if the provided codigo already exists and auto-generates
  a unique one via nextLicitacionCodigo() — codigo is now optional
  in the tool definition, type, parser, RPC, and server function
- Add advanced filters to app.transferencias: medication search,
  origin/destination warehouse selectors, and date range picker
